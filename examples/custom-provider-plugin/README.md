# custom-provider-plugin (example)

Reference implementation of a Model Provider built against `sdk/aep-provider-sdk`, kept in
lockstep with that SDK's version (`docs/architecture/02-repo-design.md` §7). Not yet implemented —
depends on `aep-provider-sdk`'s interface, which doesn't exist yet either.
