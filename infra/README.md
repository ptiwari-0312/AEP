# infra

Docker (`docker/`), Kubernetes manifests (`k8s/`), and observability config — OTel collector,
Prometheus rules, Grafana dashboards (`observability/`). Owned by platform/DevOps; never imports
application source (`docs/architecture/02-repo-design.md` §5). Empty scaffold for now.
